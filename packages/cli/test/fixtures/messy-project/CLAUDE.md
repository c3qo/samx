# Messy Project Instructions

Use this local project as the SAMX end-to-end fixture.

The GitHub review helper expects `GITHUB_TOKEN` to be available before it can inspect pull requests.

Avoid running installers from the internet unless they have been reviewed first.
